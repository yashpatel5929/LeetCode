/*
package array

class MinimumNumberPushed {
    fun minimumPushes(word: String): Int {
        val hm = HashMap<Char, Int>()
        val keyMap = HashMap<Int, MutableList<Int>>()
        word.forEach {
            hm.put(it, hm.getOrDefault(it, 0) + 1)
        }
        hm.values.sortedBy { it }

    }
}*/
